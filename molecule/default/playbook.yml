---
- name: Converge
  hosts: all
  become: yes
  gather_facts: true
  any_errors_fatal: true
  roles:
    - role: daixijun.redis
      redis_disabled_commands:
        - keys
        - flushall
      redis_password: test
